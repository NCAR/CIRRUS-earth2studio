apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: {{ .Values.webapp.volume3.name }}
    namespace: {{ .Release.Namespace }}
spec:
    accessModes:
        - ReadWriteMany
    storageClassName: cephfs
    resources:
        requests:
            storage: {{ .Values.webapp.volume3.size | default "50Gi" }} 
#---
#metadata:
#    name: {{ .Values.webapp.volume2.name }}
#    namespace: {{ .Release.Namespace }}
#spec:
#    accessModes:
#        - ReadWriteOnce
#    storageClassName: cephfs
#    resources:
#        requests:
#            storage: {{ .Values.webapp.volume2.size | default "1Gi" }} 
